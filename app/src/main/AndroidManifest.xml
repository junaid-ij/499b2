<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.junaid.apitestcopy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.AboutActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.LoginActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.RegisterActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.ICUActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.CCUActivity" />
        <activity android:name=".activity.NICUActivity" />
        <activity android:name=".activity.ICUinformation" />
        <activity android:name=".activity.ApplicationForm" />
        <activity android:name=".activity.CCUinformation" />
        <activity android:name=".activity.NICUinformation" />
        <activity android:name=".activity.Notification"></activity>
    </application>

</manifest>